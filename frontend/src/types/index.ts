export interface Driver {
    name: string,
    dob: string,
    short_name: string,
}

export interface Constructor {
    name: string,
}

export interface RaceTrack {
    name: string,
    country: string,
}
